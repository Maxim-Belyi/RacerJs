{
  "name": "racer-js",
  "version": "1.0.0",
  "description": "2D survival racing game",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:yandex": "npm run build && cd dist && zip -r ../yandex-build.zip ./*",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.69.7",
    "vite": "^5.4.0"
  }
}
